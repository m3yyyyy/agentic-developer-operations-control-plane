PORT=3100
# Optional only when integrating a live AI Gateway model outside the public demo.
AI_GATEWAY_API_KEY=
# Recommended for cryptographically binding live AI SDK approvals.
TOOL_APPROVAL_SECRET=

